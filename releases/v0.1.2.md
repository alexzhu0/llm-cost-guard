# llm-cost-guard v0.1.2

Supporting release for prompt and coding-agent context budget checks.

## Changed

- Clarified the project as a CI budget guard.
- Added README guidance for use with `repo-to-ai-brief`.
- Updated launch copy around budget discipline for long prompts and agent context.

## Try It

```bash
PYTHONPATH=src python3 -m llm_cost_guard examples/prompt.txt --model generic --budget 0.001
```
