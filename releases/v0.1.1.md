# llm-cost-guard v0.1.1

## What Changed

- Added model presets.
- Made manual `--rate` an override instead of the only path.
- Added model and rate fields to output.

## Quickstart

```bash
PYTHONPATH=src python3 -m llm_cost_guard examples/prompt.txt --model generic --budget 0.001
```

## Verification

```bash
PYTHONPATH=src python3 -m unittest discover -s tests
```
